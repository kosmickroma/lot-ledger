DB_HOST=your-cloud-sql-ip
DB_PORT=5432
DB_NAME=lotledger
DB_USER=postgres
DB_PASSWORD=your-db-password
PORT=8000

# Propelio (per-address comp/CMA API; uses Mike's authorized account)
PROPELIO_USERNAME=your-propelio-email@example.com
PROPELIO_PASSWORD=your-propelio-password
# Optional outbound proxy. Leave blank for direct connection.
PROPELIO_PROXY_URL=
